<view class="body">
<swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{imgUrls}}">
    <swiper-item>
      <image src="{{item}}" class="slide-image" />
    </swiper-item>
  </block>
</swiper>
  
<view class="section">
  <!-- <view class="title-main-before"></view> -->
  <text class="title-main">小孙水果店</text>
  <!-- <view class="title-main-after"></view> -->
</view>

<view class="section intro">
  <button class="page-body-button" type="primary" bindtap="openLocation">查看位置</button>
</view>

</view>